---
phase: 1
plan: 1
wave: 1
---

# Plan 1.1: Project Infrastructure & Testing Setup

## Objective
Establish the testing infrastructure for both frontend and backend to enable safe refactoring and stabilization.

## Context
- .gsd/SPEC.md
- .gsd/ARCHITECTURE.md
- backend/package.json
- frontend/package.json

## Tasks

<task type="auto">
  <name>Setup Vitest in Frontend</name>
  <files>frontend/package.json, frontend/vite.config.js</files>
  <action>
    Install vitest and @testing-library/react. Configure vite.config.js to support testing. Add a sample test to verify setup.
  </action>
  <verify>cd frontend && npm test -- --run</verify>
  <done>Vitest is installed and a sample test passes.</done>
</task>

<task type="auto">
  <name>Setup Jest and Supertest in Backend</name>
  <files>backend/package.json, backend/jest.config.js</files>
  <action>
    Install jest, supertest, and cross-env. Create a jest.config.js. Add a sample API test (e.g., checking /api/v1/auth status).
  </action>
  <verify>cd backend && npm test</verify>
  <done>Jest is installed and a sample API test passes.</done>
</task>

## Success Criteria
- [ ] Frontend can run unit tests with `npm test`.
- [ ] Backend can run integration tests with `npm test`.
- [ ] Both environments have a working example test.

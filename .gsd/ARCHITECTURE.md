# Architecture

> Auto-generated by /map on 2026-05-05

## Overview

FudPro (Mumbai Dabbawala System) is a modular monolith application designed to manage a food delivery ecosystem involving Customers, Vendors, and Admins. The system is split into a React-based frontend and a Node.js/Express-based backend, communicating via a RESTful API.

```
┌─────────────────────────────────────────┐
│              [React SPA]                │
│         (Vite + Mantine + Zustand)      │
├─────────────────────────────────────────┤
│              [REST API]                 │
│         (Express.js + Sequelize)        │
├─────────────────────────────────────────┤
│            [PostgreSQL DB]              │
└─────────────────────────────────────────┘
```

## Components

### Frontend (frontend/)
- **Core:** React 19 with Vite 8.
- **UI Framework:** Mantine 9 for components and styling.
- **State Management:** Zustand for global application state.
- **Animations:** Framer Motion for premium UI interactions.
- **Routing:** React Router Dom 7 for client-side navigation.
- **I18n:** react-i18next for multi-language support.

### Backend (backend/)
- **Core:** Express.js 4.
- **ORM:** Sequelize 6 interacting with PostgreSQL.
- **Authentication:** JWT (jsonwebtoken) and bcryptjs.
- **Logging:** Winston for system logs.
- **Documentation:** Swagger (swagger-ui-express) for API documentation.
- **File Handling:** Multer for media uploads.
- **Mailing:** Nodemailer for email notifications.

### Data Model
- **Auth:** Roles (Admin, Superadmin, Vendor, Customer).
- **Users:** User profiles and authentication data.
- **Vendors:** Restaurant/Dabbawala profiles.
- **Menu:** Categories and Menu Items.
- **Orders:** Order lifecycle management.
- **Media:** Storage for images and assets.

## Data Flow

1. **User Interaction:** User interacts with the Mantine-based UI.
2. **Action Trigger:** UI components trigger logic which may call Zustand store actions or hooks.
3. **API Request:** Frontend sends HTTP requests using Axios to the `/api/v1` endpoints.
4. **Middleware:** Backend processes requests through loaders (Express, Routes) and shared middleware (Auth, Error).
5. **Controller/Service:** Routes map to controllers which delegate business logic to services.
6. **Persistence:** Services use Sequelize models to query or mutate the PostgreSQL database.
7. **Response:** Backend returns JSON responses which the frontend uses to update its state and UI.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| PostgreSQL | DB | Primary relational data storage |
| Swagger | API Docs | Interactive API documentation at `/api-docs` |
| Nodemailer | SMTP | Transactional email delivery |

## Technical Debt

- [ ] Consolidate role-based logic in frontend pages to reduce duplication.
- [ ] Implement robust frontend error boundaries.
- [ ] Add unit and integration tests for critical business logic in backend services.
- [ ] Ensure consistent input validation across all API endpoints.

## Conventions

**Naming:** CamelCase for frontend components, camelCase for functions/variables, snake_case or camelCase for DB fields (Sequelize handles underscored: true).
**Structure:** Modular structure for both frontend and backend (modules/auth, modules/order, etc.).
**Testing:** Currently minimal testing observed; need to establish patterns for Jest or Vitest.
